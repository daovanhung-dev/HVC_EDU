import { CommonModule } from '@angular/common';
import { Component, OnInit, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { SupabaseService } from '../../core/supabase/supabase.service';
@Component({selector:'app-audit',standalone:true,imports:[CommonModule,FormsModule],template:`
<section class="page-header"><div><p class="eyebrow">HỆ THỐNG</p><h1>Nhật ký audit</h1><p class="muted">Theo dõi các mutation quan trọng, traceId và before/after.</p></div><button class="secondary" (click)="load()">Làm mới</button></section><div class="toolbar"><label>Action<input [(ngModel)]="action" placeholder="PAYMENT_CREATED" /></label><label>Resource<input [(ngModel)]="resource" placeholder="payment" /></label></div>@if(error()){<div class="alert">{{error()}}</div>}<div class="card table-wrap"><table><thead><tr><th>Thời gian</th><th>Action</th><th>Resource</th><th>Actor</th><th>Trace ID</th><th>After</th></tr></thead><tbody>@for(item of filtered();track item.id){<tr><td>{{item.created_at|slice:0:19}}</td><td>{{item.action}}</td><td>{{item.resource_type}} / {{item.resource_id}}</td><td>{{item.actor_user_id}}</td><td><code>{{item.trace_id}}</code></td><td><code>{{item.after_data|json}}</code></td></tr>}@empty{<tr><td colspan="6" class="empty">Chưa có audit log.</td></tr>}</tbody></table></div>
`})
export class AuditComponent implements OnInit{items=signal<any[]>([]);error=signal('');action='';resource='';constructor(private readonly supabase:SupabaseService){}ngOnInit(){void this.load();}async load(){const r=await this.supabase.client.from('audit_logs').select('*').order('created_at',{ascending:false}).limit(200);if(r.error)this.error.set(r.error.message);else this.items.set(r.data||[]);}filtered(){return this.items().filter(x=>(!this.action||x.action.toLowerCase().includes(this.action.toLowerCase()))&&(!this.resource||x.resource_type.toLowerCase().includes(this.resource.toLowerCase())));}}
